# recon 1.0 — release design

**Date:** 2026-09-11  
**Status:** approved by Pete Richardson on 2026-09-11. Amended on 2026-09-11 to add phase 1b, after phase 1 merged, and again to record the phase 2 design.  
**Next step:** an implementation plan, one per phase

## 1. Why this document

recon is at version 0.1.0. The repository has no tag and no release, and 47 issues are open. This document says which of them 1.0 contains, in what order they are done, and how you know that the release is ready.

## 2. What 1.0 promises

1.0 means that recon is complete and reliable enough to depend on every day. It is **not** a compatibility contract: a 1.x release can still change a key, a flag or a configuration key.

The absence of a contract is the reason for the order of the work. A change that makes a user relearn something must land **before** 1.0, and not after it. A change that only adds something new can wait, because it takes nothing away.

## 3. Scope

**In: 46 issues** — 7 features and 39 consolidation items (32 small, 7 medium).  
**Out: 6 issues.**

The two counts cover 52 issues: the 47 that were open when this document was written, the 4 that phase 1's own review found, and the 1 that the key survey for phase 2 found.

| # | Title | Why it waits |
|---|---|---|
| #242 | Hex view: render any file as offset, hex bytes and ASCII | The feature is additive, and 1.1 can add it without taking anything away. Only its key must be decided in 1.0, and phase 2 does that. |
| #38 | Interactive regex builder: live preview with a back button | A new mode beside the plain edit flow, which stays as it is. Nothing is lost if it comes later. |
| #10 | Add an image viewer for terminals that support inline graphics | Additive, and the terminal graphics protocols are an unknown quantity. The issue itself gives no estimate. |
| #19 | Restore the >> selection marker as a configurable option (default off) | Pete's decision on 2026-09-11: a second selection cue is a distraction to maintain, whatever its default. |
| #247 | --print-editor-config no longer refuses an unknown --set, so a typo exits 0 | Severity low, and the issue needs a decision on which behaviour is correct before anybody writes code. Exit criterion 2 bars severity medium and above only. |
| #248 | "cannot open {path}: no such file" puts the reason last, so a long path truncates it away | Severity low. The message is still complete on a terminal of usual width. |

## 4. How the order is decided

Three rules set the phases.

1. **A large change waits for the debt in the files that it touches.** You clean a module one time, and then you change it. This is the reason that #160 comes before #40.

2. **The keymap comes early.** After the actions have names and a user can rebind them, a later key change stops hurting: a user who dislikes 1.1 puts the old key back in the configuration file. #59 is thus the highest-leverage item in the release.

3. **Documentation comes last**, except where a refactor physically moves the text. The nine drift issues describe code that phases 2 to 4 change. If you correct them first, you correct them two times.

## 5. The six phases

Phase 1b is a follow-on to phase 1 and not a seventh phase. Phase 1's review found the two issues in it, and the numbers of phases 2 to 6 stay as they are.

### Phase 1 — Visible bugs and cheap correctness (12)

| # | Effort | Title |
|---|---|---|
| #243 | S | A / search from the navigator that matches no filename is silent |
| #222 | S | Error hooks write LeaveAlternateScreen to stderr even when no terminal was initialised |
| #205 | S | Commented-out terminal.show_cursor() in restore_terminal has no rationale |
| #157 | S | CLI argument is loaded relative while the navigator is absolute, so the changed-on-disk badge never fires for it |
| #191 | S | A malformed filters.toml blocks --print-editor-config |
| #203 | S | ProfilePicker::render uses unchecked u16 subtraction on the render path |
| #193 | S | The profile picker ignores modifiers and measures width in chars, missing the #120 and #97 rules |
| #187 | S | recompile swallows the RegexSet error, silently turning navigator marking off |
| #189 | S | Preview and directory-listing read failures are not logged, unlike read_lines |
| #188 | S | Scanner thread is spawned unnamed and a panicking worker never disconnects its channel |
| #183 | S | Highlighter resync overwrites already-exact spans with approximate ones |
| #196 | S | fileview.rs and filenav.rs import color_eyre::Result and never use it |

Everything that a user meets today. #222 and #205 change the same function, `restore_terminal`, so they go together. #191 also opens the way for #46 in phase 5, because both read `filters.toml`.

### Phase 1b — The live screen (2)

| # | Effort | Title |
|---|---|---|
| #246 | M | In-session log records reach stderr while the TUI holds the alternate screen |
| #245 | M | A background-thread panic tears down the live TUI and consumes the one-shot terminal undo |

Phase 1's review found both of them, after phase 1 merged. They belong together because they obey one rule: while the alternate screen is up, nothing writes to it but the drawing code. #246 gives that rule to the logger, and #245 gives the same rule to the panic hook.

Do #246 first. The panic hook in #245 writes its record through the logger that #246 builds.

**#246.** While recon holds the alternate screen, a log record does not go to stderr. If `RECON_LOG` names a file, every record goes to the file, as it does today. The records at startup and at shutdown keep their present behaviour, because the screen is not up at those times.

**#245.** A panic on a worker thread ends that thread quietly. It does not restore the terminal and it does not print a report, so the screen that the user looks at stays as it is. A panic on the main thread keeps today's behaviour exactly: recon restores the terminal, and then prints the report.

**The cost, which Pete accepted.** With both changes and no `RECON_LOG`, a panic in the scan worker leaves no trace. The user sees only that the marks on the files do not change. `RECON_LOG` makes the panic and its position visible again, in the file.

### Phase 2 — Keymap architecture (5)

| # | Effort | Title |
|---|---|---|
| #59 | — | provide names for all keymap-able functions |
| #61 | — | add keymap overrides to the config file |
| #250 | S | View-focused G jumps to the end of the loaded buffer, not the end of the document |
| #199 | M | too_many_lines rationale ('one place to read the whole keymap') no longer holds — the keymap is in six places |
| #178 | S | Comments misstate when crossterm reports SHIFT; the legacy-key-mode dependence of the is_empty() guards is unrecorded |

#59 gives every action a name. #61 then lets a user rebind it. #199 and #178 are the comments that describe this code, and #59 rewrites the code under them. A key survey for this phase found #250; the same work removes its cause.

The survey also corrected two things that this document said. The keymap is in **eight** places and not six: the profile picker binds its own keys, and the prompt handler binds ten more while a prompt is open. And "chain" is not a layer while the program runs: `f` moves the focus, and the next key is then an ordinary key of the pane that has the focus.

**One table.** Each entry gives a scope, a key and the name of an action. The table that recon is built with is the only list, so the help overlay, the README and the dispatch all read the same data. This makes the statement in #199 true again.

**Five scopes, in the order that recon already uses:** prompt, help, picker, global, then the pane that has the focus. A modal scope takes every key while it is open.

**A name carries its scope** — `view.goto.end`, `nav.open`, `global.quit`. The scope is necessary and not decoration: `g` and `G` mean "the first row" and "the last row" in three different panes, with three different handlers. They are thus three actions and not one.

**Modifiers become standard.** Today two different tests guard the keys, and the wrong choice between them is the cause of #250. The table makes the key standard when it goes in: `SHIFT` is removed when the character is already a capital, because in the legacy key mode `SHIFT` tells you nothing there, and `CONTROL` and `ALT` must agree exactly. `G` then finds one entry, whatever the terminal sends, so #250 cannot occur again.

**The user's keys go in a `[keymap]` table in `config.toml`**, beside `[editor]`, `[clipboard]` and `[syntax]`. An entry gives an action and one key or more. recon reads the file and does not write it, as it does today. A new `--print-keymap` prints the whole default table in the same syntax, so a user can copy a line and change it. Recon tests the names and the keys when it reads the file, and not when a key is pressed.

**Two keys are reserved and no action uses them:** `-` for the hex view (#242) and `:` for a command palette. #120 left only `-`, `=`, `;`, `'`, `` ` `` and `\` free. Both keys are recorded in the documentation as taken, because a key that 1.1 adds must be a key that 1.0 already promised. If a user's file binds a reserved key, recon gives a warning and then obeys the user.

The command palette itself is not in 1.0. Only the architecture that makes it possible, and its key.

### Phase 3 — Filter groups (4)

| # | Effort | Title |
|---|---|---|
| #160 | M | filter.rs does four jobs in one 3,700-line module |
| #40 | — | Filter groups: AND within a group, OR between groups |
| #179 | S | Six doc comments are attached to the wrong item |
| #177 | S | Saved-filter-sets spec still says there are no digit bindings |

#160 splits `filter.rs` into four modules first. #40 then changes what a filter number means, in a module that is clean. #179 and #177 are the documents that this work moves.

### Phase 4 — Performance and memory (10)

| # | Effort | Title |
|---|---|---|
| #170 | S | poll_stamps builds a Matcher sixty times a second just to test is_none() |
| #172 | S | sort_key allocates a lowercase String and clones the OsString for every navigator entry |
| #186 | S | refresh_scan allocates pattern_key per event; App::new rebuilds the highlighter twice at startup |
| #204 | S | FilterList walks its rows three times per frame (texts → numbered → rows, plus preferred_width) |
| #168 | M | apply_syntax re-runs the search regex over every window row on every frame |
| #184 | S | KindScopes::note allocates a String per open scope per token to test a 'meta.' prefix |
| #98 | S | apply_pending_scroll does a throwaway full render to prime the viewport |
| #195 | M | The directory-listing renderer lives in fileview.rs while Entry and sorted_entries live in filenav.rs |
| #156 | M | refresh_scan and check_stamps stat every listed file synchronously on the UI thread |
| #51 | — | Hold a line index instead of the file's lines (index-only Document) |

The small wins come first. #156 then moves the file checks off the screen thread. #51 is last, because it is the largest item in the release.

### Phase 5 — Config, headless and scripts (4)

| # | Effort | Title |
|---|---|---|
| #46 | — | RECON_FILTER_PATH: a PATH-style list of filter-set directories |
| #224 | M | Headless --emit files answers definition filters by parsing, not scanning |
| #219 | M | --emit lines buffers the whole output before writing; \| head reads the entire file |
| #223 | S | Headless spec's Errors section should attribute the binary-file refusal to --emit lines only |

#224 and #219 change the same two files, `headless.rs` and `emit.rs`, which is why #219 is cheap here. #224 is the one item in the release that changes an answer that a script already depends on.

### Phase 6 — Documentation and release hygiene (9)

| # | Effort | Title |
|---|---|---|
| #180 | S | Five doc statements contradicted by adjacent code (evaluate cost, line_styles, row_count/len, 'a screenful', 'Unreachable') |
| #176 | S | README warn-level logging row omits three sources; editor.rs comment says debug! over a warn! call |
| #175 | S | README Known Limitations says 'Nothing is persisted' 850 lines after documenting saved filter sets |
| #174 | S | README Development section describes the August codebase: test counts, AppWidget suppression, layout table |
| #99 | S | Ignore /code-review snapshots without ignoring PROJECT_REVIEW.md |
| #198 | S | Cargo.toml declares no rust-version; the 1.88 floor lives only in the vendored fork |
| #197 | S | CI runs neither cargo audit nor the ignored real-process editor test |
| #202 | S | The --theme long help (bundled theme names) has no test |
| #201 | S | Nothing proves the scanner's per-line cancel check fires mid-file |

The README is written against the code that phases 1 to 5 produced. #198 and #197 fix the build floor and the CI checks, which is what you want immediately before a tag.

## 6. Exit criteria

1.0 is ready when all six are true.

1. All 46 issues are closed.
2. No open issue has severity medium or above. This includes an issue that the work itself finds, and not only the 46 issues in the list. #245, #246 and #250 are this criterion at work: the reviews and surveys for this release found all three, each is severity medium, and each went into the phase that removes its cause.
3. The README describes the code as it is. Today its Known Limitations section cites six issues (#7, #8, #18, #27, #70, #165) that are all closed.
4. CI runs `cargo audit` and the one `#[ignore]`d real-process test.
5. `Cargo.toml` says `version = "1.0.0"` and declares `rust-version`.
6. A git tag and a GitHub release exist. The repository has neither today.

## 7. Risks

**#51 is the largest item.** It needs a background read, because a filter change must not stop the screen on a large file. It is cheaper than its own text says: the issue states that recon has no threading, but recon now has a scan worker (`src/scan.rs:160`) and an editor launcher (`src/editor.rs:518`), both with channels. It is still the item most likely to move the date.

**#40 changes what a filter number means.** `Verdict::Included` becomes a group index and not a filter index. #180 records a trap in that exact area, so phase 3 must correct the document and the code together.

## 8. Decisions and their reasons

| Decision | Reason |
|---|---|
| 1.0 is not a compatibility contract | Pete's choice: 1.0 says "ready for daily use". |
| #46 and #51 stay in 1.0 | Pete's choice. Both were already in the 1.0 milestone. |
| #219 is in 1.0 | Pete's choice: a large improvement for `\| head` on a big file. |
| #224 is in 1.0 | It is the only item that changes an answer a script depends on. A script written for 1.0 would get a different file list in 1.1. |
| #242 is out, but its key is chosen in 1.0 | The feature is additive. The key is not: no letter is free. |
| #19 is out | Pete's choice: a second selection cue is a distraction to maintain. |
| A log record is suppressed while the TUI is up, unless `RECON_LOG` names a file | Pete's choice: a record drawn over the screen costs more than the record is worth, and one variable gets it back. |
| A panic on a worker thread dies quietly, and the main thread keeps today's behaviour | Pete's choice: the screen that a user looks at must survive a thread that a user cannot see. |
| With no `RECON_LOG`, a panic in a worker leaves no trace | Accepted cost. Pete chose it in place of a buffer that holds the records until the screen goes away. |
| Phase 2 resolves a key to an action, and then performs it | Two other ways were weighed. A layer that only translates one key to another is cheaper, but it leaves the keymap in seven places and does not remove #250. A full command pattern at every one of the seven places is the largest change in the release, and 1.0 does not need it. |
| `-` is the hex-view key | Pete's choice, recorded on #242. |
| The command palette is out of 1.0, but `:` is reserved for it | Pete's choice. The same rule as the hex view: a key that 1.1 adds must be a key that 1.0 already promised. Cmd-Shift-P cannot be used, because a terminal in the legacy key mode never reports the Command key. |
| The user's keys go in `config.toml` and not in a second file | Pete's choice. An override is a few lines, so it suits a table in the file that recon already reads. |
| A user may bind a reserved key; recon gives a warning | Pete's choice for 1.0. The warning tells the user what 1.1 will do, and it does not take the key away. |
| #250 is fixed in phase 2 | Pete's choice. The phase makes one place that resolves a key, so the fault cannot come back. |

## 9. After 1.0

1.1 and later take the six deferred issues, and anything new. Because 1.0 makes the actions nameable and rebindable, a key change after 1.0 is a change that a user can undo alone.

1.0 also promises two keys to 1.1: `-` for the hex view, and `:` for a command palette. Both are recorded as taken, and no action in 1.0 uses them.
