# recon

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `PeteRichardson/recon`, through the `gh` CLI.
See `docs/agents/issue-tracker.md`.

### Triage labels

The five default labels, each one named for its role. See
`docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See
`docs/agents/domain.md`.
